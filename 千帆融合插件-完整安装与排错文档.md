# 千帆融合插件 · 完整安装与排错文档

> **用途**：在另一台电脑上完成千帆融合插件的安装配置。本文档记录了从安装到报错修复的完整过程，包含所有踩坑与解决方案。
>
> **前置条件**：已安装 DSH（`@deepseek-ai/dsh`，版本 ≥ 0.1.1-rc.2）、已安装 git（配置好代理 `http://192.168.2.27:7890` 或能直连 GitHub）、本机有 curl。

---

## 一、安装过程

### 1.1 克隆仓库

```bash
git clone https://github.com/Kiva007/dsh-llm-qianfan.git -b feat/pr-reasoning-effort-and-tokenplan-monorepo
```

拉下来后 `lib/` 已构建好，**不需要装 pnpm / tsdown / TypeScript**。
但注意：克隆后**必须先跑一次 `pnpm install`**（见第二章报错说明），否则插件依赖缺失会导致启动失败。

```bash
cd dsh-llm-qianfan
pnpm install
```

> 仓库内的 peerDependencies 已提升到 `^0.1.1-rc.2`（commit `516f307`），`pnpm install` 会把 `@deepseek-ai/dsh-llm` 等正确解析到与 harness 匹配的新版，**不再需要任何手工目录联接（Junction）**。

### 1.2 安装插件

```bash
# ⚠️ 原始指南的命令缺少 --profile web，必须补上，否则报错找不到 profile
# 适配器（推理档位 + rateLimit 机制 + 千帆 API 对接）
dsh plugin --profile web add ./packages/dsh-llm-qianfan

# tokenplan（余量侧边栏卡片 + 设置页含速率限制表单）
dsh plugin --profile web add ./packages/dsh-qianfan-tokenplan
```

### 1.3 配置 settings.yaml

在 `~/.dsh/settings.yaml` 中追加以下内容（完整 7 模型 + 推理档位 + 速率限制）：

```yaml
llm-qianfan:
  apiKeyEnv: QIANFAN_API_KEY
  baseURL: https://qianfan.baidubce.com/v2/tokenplan/personal
  models:
    - id: deepseek-v4-pro
      name: DeepSeek-V4-Pro
      description: 闲时2折 · 1M 上下文 / 最大输出 384K · 适用于复杂推理、深度数据分析、多步骤智能体工作流
      contextWindow: 1048576
      maxTokens: 393216
      thinking: true
      reasoningEfforts:
        off: null
        high: high
        max: max
    - id: deepseek-v4-pro-0813
      name: DeepSeek-V4-Pro-0813
      description: 1M 上下文 / 最大输出 384K · V4-Pro 正式版本（1.8x 抵扣系数），增强代理功能与生产环境表现
      contextWindow: 1048576
      maxTokens: 393216
      thinking: true
      reasoningEfforts:
        off: null
        high: high
        max: max
    - id: deepseek-v4-flash
      name: DeepSeek-V4-Flash
      description: 闲时2折 · 1M 上下文 / 最大输出 384K · 高效版，兼顾推理效果与响应效率，支持超长上下文
      contextWindow: 1048576
      maxTokens: 393216
      thinking: true
      reasoningEfforts:
        off: null
        high: high
        max: max
    - id: deepseek-v4-flash-0731
      name: DeepSeek-V4-Flash-0731
      description: 闲时2折 · 1M 上下文 / 最大输出 384K · V4-Flash 正式版本，取代预览版，大幅增强智能体能力
      contextWindow: 1048576
      maxTokens: 393216
      thinking: true
      reasoningEfforts:
        off: null
        high: high
        max: max
    - id: glm-5.2
      name: GLM-5.2
      description: 闲时2折 · 1M 上下文 / 最大输出 128K · 智谱 AI 新一代旗舰模型，专注于长程任务与复杂工程场景
      contextWindow: 1048576
      maxTokens: 131072
      thinking: true
      reasoningEfforts:
        off: null
        high: high
        max: max
    - id: glm-5.1
      name: GLM-5.1
      description: 198K 上下文 / 最大输出 128K · 智谱专为智能体工程设计的旗舰模型
      contextWindow: 202752
      maxTokens: 131072
      thinking: true
      reasoningEfforts:
        off: null
        high: high
        max: max
    - id: kimi-k2.6
      name: Kimi-K2.6
      description: 256K 上下文 / 最大输出 256K · 支持文本图片输入、思考与非思考模式
      contextWindow: 262144
      maxTokens: 262144
      thinking: true
      reasoningEfforts:
        off: null
        high: high
        max: max
  reasoning: max
  rateLimit:
    tpm: 1000000
    rpm: 50
    safetyMargin: 0.1
    minIntervalMs: 200
```

### 1.4 配置凭据

在 `~/.dsh/.credentials.yaml` 的 `refs:` 下追加：

```yaml
  QIANFAN_API_KEY: bce-v3/ALTAKSP-<你的密钥>
  QIANFAN_TP_COOKIE: 'Cookie: <从千帆控制台获取的完整 Cookie 字符串>'
```

> Cookie 过期后可到设置 → 千帆 Token Plan 页面粘贴新 Cookie，或直接编辑 `.credentials.yaml`。
> 该文件包含敏感凭据，已加入 `.gitignore`（`QIANFAN-PRIVATE-INSTALL.md` 同理，勿提交入库）。

### 1.5 编码注意事项（Windows 中文系统）

本机为 Windows 中文系统（代码页 936/GBK）。编辑 settings.yaml / .credentials.yaml 时：

- **文件编码必须为 UTF-8（无 BOM）**
- 如果用 PowerShell 读取/写入含中文的文件，**必须显式指定 `-Encoding UTF8`**，否则 PowerShell 默认按 GBK 解码，会把 UTF-8 中文读成乱码写入文件
- 验证方法：读取后检查中文 description 是否正常显示（如"闲时2折"而非乱码）

### 1.6 重启与验证

安装+配置完成后重启 dsh web + 浏览器 Ctrl+Shift+R 强刷，检查：

1. **模型选择器**：千帆模型应出现「推理等级」菜单（Off / High / Max），预选 Max
2. **侧边栏**：底部应有千帆 Token Plan 余量横条卡片（套餐类型 / 剩余% / 进度条）
3. **设置页**：千帆 Token Plan 卡片应含 Cookie 输入 + 速率限制表单（TPM/RPM/安全余量/最小间隔）
4. **发一条请求**：选千帆模型发消息，确认正常回复 + reasoning_content 出现（Max 档位）

---

## 二、第一轮报错：Cannot find package（外部依赖缺失）

### 2.1 报错现象

重启 dsh web 后启动直接失败，报错：

```
Error: dsh: plugin tree failed to load: failed to apply loader entry include (cordis:include): loader entries failed to apply

Error: failed to import loader entry llm-qianfan (dsh-llm-qianfan):
  Cannot find package '@deepseek-ai/schemastery' imported from
  <工作区>\dsh-llm-qianfan\packages\dsh-llm-qianfan\lib\index.js
  code: 'ERR_MODULE_NOT_FOUND'

Error: failed to import loader entry qianfan-tokenplan (dsh-qianfan-tokenplan):
  Cannot find package '@deepseek-ai/dsh-credentials' imported from
  <工作区>\dsh-llm-qianfan\packages\dsh-qianfan-tokenplan\lib\index.js
  code: 'ERR_MODULE_NOT_FOUND'
```

### 2.2 根因

两个千帆插件是通过 `dsh plugin add ./local/path` 装的，dsh 用 `link:` 协议把它加进 profile——但 **`link:` 只建符号链接，不安装插件自己的 dependencies**。而 `git clone` 后没跑 `pnpm install`，所以插件 import 的 `@deepseek-ai/schemastery`、`@deepseek-ai/dsh-credentials`、`zod`、`eventsource-parser` 都没装，node 解析不到。

对比：其他插件（cost-meter、memory-evolve 等）是完整 npm/github 依赖树，pnpm 自动装了依赖，所以不受影响。

### 2.3 修复

在本地 monorepo 根目录跑 `pnpm install`（它有 pnpm-workspace.yaml / pnpm-lock.yaml），装齐依赖：

```bash
cd <工作区>\dsh-llm-qianfan
pnpm install
```

`@deepseek-ai/*` 类是公开包，registry 直接拉取成功，dependencies + peer 都自动装上（isolated 模式装到各 package 自己的 node_modules）。

### 2.4 验证

- `dsh --profile web --dump-default-config`：两个入口都注册——`id: llm-qianfan` + `id: qianfan-tokenplan`
- 服务器日志：`Cannot find package` / `ERR_MODULE_NOT_FOUND` / `plugin tree failed` → 0 处
- 依赖解析测试：从两个插件目录 `require.resolve('@deepseek-ai/schemastery')`、`dsh-credentials`、`zod` 全部能解析

### 2.5 教训

**以后凡是本地路径装 dsh 插件（`dsh plugin add ./xxx`），记得先在那个目录跑一次 `pnpm install`，否则 `link:` 不装依赖就会重现这个错。**

---

## 三、第二轮报错：prepareCall is not a function（内部版本不匹配）——已根治

### 3.1 报错现象

`pnpm install` 修复后 dsh web 能正常启动，但**发送对话时报错**：

```
本轮运行失败
registration.adapter.prepareCall is not a function
```

### 3.2 根因分析（完整链路）

这是**插件与 dsh 核心版本不匹配**导致的，根因链条如下：

1. **dsh 0.1.1-rc.2 的 agent loop** 会无条件调用 `ctx.llm.prepareCall()`（`dsh-llm/lib/index.js:1498`），进而调用 `registration.adapter.prepareCall(provider, model, signal)`——这是推理档位（reasoning effort）机制的一部分。
2. **千帆插件的 `QianfanAdapter extends LlmAdapter`**——它继承了哪个基类取决于运行时 Node 的模块解析结果。
3. **插件的 peerDependencies 声明** 当初是 `@deepseek-ai/dsh-llm: ^0.1.0-rc.7`，`pnpm install` 按此范围装了**旧版 0.1.0-rc.7** 到插件嵌套 `node_modules`。
4. **Node 的就近解析原则**：插件目录下的 `node_modules/@deepseek-ai/dsh-llm`（0.1.0-rc.7）**优先于** harness 自带的 0.1.1-rc.2 被加载。
5. **旧版基类 `LlmAdapter` 没有 `prepareCall` 方法**（0.1.0-rc.7 只在 `LlmRuntime` 层有调用点，基类本身未提供默认实现）→ 适配器对象上不存在该方法 → 报错。
6. **新版基类（0.1.1-rc.2）自带 `prepareCall` 默认实现**：
   ```js
   async prepareCall(provider, model, signal) {
       return {
           model: await this.resolveModel(provider, model, signal),
           stream: (options) => this.stream(options)
       };
   }
   ```
   子类无需覆写即可工作。

### 3.3 根治方案（commit 516f307：peer 提升到 ^0.1.1-rc.2）

**仓库已在 commit `516f307` 把两个插件包的 peerDependencies 从 `^0.1.0-rc.7` 提升为 `^0.1.1-rc.2`**。此后：

1. 拉取/更新该分支；
2. 在仓库根跑一次 `pnpm install`——pnpm 会按新 peer 范围把 `@deepseek-ai/dsh-llm` 解析到 **0.1.1-rc.2（与 harness 一致）**，插件嵌套依赖不再是旧版；
3. 必要时重新 `dsh plugin --profile web add ./packages/...` 刷新 profile 快照；
4. 重启 dsh web 即可。新版基类自带 `prepareCall` 默认实现，子类无需覆写即可工作。

验证（在仓库根）：

```bash
node -e "const m = require('./packages/dsh-llm-qianfan/lib/index.js'); console.log('QianfanAdapter.prepareCall:', typeof m.QianfanAdapter.prototype.prepareCall)"
```

预期输出：`QianfanAdapter.prepareCall: function`

### 3.4 ⚠️ 旧方案（Junction 目录联接）已废弃，不要再使用

此前因 pnpm 无法把嵌套 dsh-llm 解析到新版，曾用**把插件嵌套 `node_modules/@deepseek-ai/dsh-llm` 手工替换为指向 harness 新版目录的 Junction** 的临时方案（`cmd /c rmdir` + `cmd /c mklink /J`）。该方案在 `516f307` 根治后**已不再需要**：

- Junction 是手工残留，`pnpm install` / `dsh plugin add` 都会把它还原成旧版符号链接，需要反复重打，是维护陷阱；
- 现在 peer 范围正确，`pnpm install` 一次到位，无需任何手工操作；
- 若旧机器上残留了 Junction，删除方式：`cmd /c rmdir "<插件目录>\node_modules\@deepseek-ai\dsh-llm"` 后重新跑 `pnpm install`。

---

## 四、第三轮修复：tool_calls 序列化（commit 12a8b9a）

### 4.1 报错现象（上下文污染 / 异常截断）

使用千帆模型（glm-5.2 / deepseek-v4-*）的会话出现以下异常：

1. **PowerShell 等工具参数被逐字符拆成单行**；
2. **DSML 工具调用标签竖线被腐蚀**（单竖线 `｜` → 双竖线 `｜｜`，参数逐字符拆解）；
3. **回复在工具调用中途末尾截断**；
4. **会话存储膨胀**：`~/.dsh/sessions/**/session.jsonl.zstd` 从正常 ~200KB 涨到 6MB+（15MB 解压后），对应 input tokens 暴涨（如 79 次调用 872 万 tokens，均 11 万/次）。

### 4.2 根因

千帆适配器 `src/serialize.ts` 的 `serializeRequest()` 序列化 messages 时**只提取 `role` + `content`**（`normalizeContent` 只提取 text 块，无 text 时 `JSON.stringify` 整个 content 数组），导致：

- **assistant 消息的 `tool_calls` 字段被丢弃**；
- **tool 消息的 `tool_call_id` 字段被丢弃**；

模型在对话历史里看不到合法的 tool call，只看到 JSON 纯文本形式的 content → 模型学样输出腐败的 DSML 文本 → 上下文污染恶性循环。

对比：标准 OpenAI 适配器（pi-ai `openai-completions.js:888`）会把 `tool_calls` 作为独立字段发送。

### 4.3 修复内容（commit 12a8b9a）

重写 `serializeMessage`：

- **assistant 的 tool-call 块** → 提取为独立的 `tool_calls` 数组字段发送；
- **tool-result 消息**（`content[0].type === 'tool-result'`）→ 转为 `role: "tool"` 并携带 `tool_call_id`；
- **空 assistant 消息**（无 content 无 tool_calls）→ 直接跳过不发送。

### 4.4 验证与注意事项

- 已在本机通过**三轮 tool call 实测**，三种损坏模式均未复现。
- **旧会话救不了，只能救新会话**：已被污染的会话历史里存着腐败 text 块（逐字符拆解 / 双竖线 DSML / JSON tool call 文本），修复后的 `serializeMessage` 会原样发给模型。判定污染：会话存储 >1MB 或出现过上述截断症状→归档/删除后新建会话重做任务。
- 未用千帆的会话（直连 baishan / deepseek-official 等）不受影响。

---

## 五、完整操作清单（给另一台电脑的执行步骤）

按以下顺序执行，每步完成后确认再继续：

### 步骤 1：确认 dsh 版本
```bash
dsh -V
# 必须 ≥ 0.1.1-rc.2，否则先升级：npm install -g @deepseek-ai/dsh@0.1.1-rc.2
```

### 步骤 2：克隆仓库
```bash
cd <工作区目录>
git clone https://github.com/Kiva007/dsh-llm-qianfan.git -b feat/pr-reasoning-effort-and-tokenplan-monorepo
```

### 步骤 3：在仓库内跑 pnpm install（防止第一轮报错）
```bash
cd dsh-llm-qianfan
pnpm install
# 新 peer ^0.1.1-rc.2 会被正确解析，无需 Junction hack
```

### 步骤 4：安装插件到 web profile
```bash
dsh plugin --profile web add ./packages/dsh-llm-qianfan
dsh plugin --profile web add ./packages/dsh-qianfan-tokenplan
```

### 步骤 5：确认序列化修复已生效
```bash
node -e "const m = require('./packages/dsh-llm-qianfan/lib/index.js'); console.log('prepareCall:', typeof m.QianfanAdapter.prototype.prepareCall)"
# 预期输出：prepareCall: function
# 且 lib/index.js 应含 serializeMessage / tool-result 判断（commit 12a8b9a）
```

### 步骤 6：配置 settings.yaml
在 `~/.dsh/settings.yaml` 末尾追加第一章 1.3 节的 `llm-qianfan` 配置段（7 模型 + 推理档位 + 速率限制）。

### 步骤 7：配置 .credentials.yaml
在 `~/.dsh/.credentials.yaml` 的 `refs:` 下追加 `QIANFAN_API_KEY` 和 `QIANFAN_TP_COOKIE`（值见 `QIANFAN-PRIVATE-INSTALL.md` 第五节）。

### 步骤 8：重启 + 验证
```bash
dsh web
```
浏览器 Ctrl+Shift+R 强刷，按第一章 1.6 节的验证清单逐项检查。若此前用千帆跑过被污染的长会话，**新建会话**做三轮 tool call 实测。

---

## 六、故障速查表

| 症状 | 原因 | 修复 |
|------|------|------|
| 启动报 `Cannot find package '@deepseek-ai/schemastery'` | 克隆后未跑 `pnpm install` | 在仓库根目录跑 `pnpm install` |
| 启动报 `Cannot find package '@deepseek-ai/dsh-credentials'` | 同上 | 同上 |
| 启动正常但发消息报 `prepareCall is not a function` | 仓库分支太旧（peer 仍是 `^0.1.0-rc.7` 或嵌套装到旧版 dsh-llm） | 更新到含 `516f307` 的版本 + 仓库内 `pnpm install` |
| 工具参数逐字符拆解 / DSML 竖线腐蚀 / 回复中途截断 / 会话存储 >1MB | 旧适配器 serializeRequest 丢弃 tool_calls/tool_call_id，上下文被污染 | 更新到含 `12a8b9a` 的版本；污染的旧会话归档重建 |
| 模型选择器没有推理等级菜单 | settings.yaml 模型缺 `reasoningEfforts` 字段或顶层缺 `reasoning` | 检查 settings.yaml 配置完整性 |
| 侧边栏没有余量卡片 | `.credentials.yaml` 缺 `QIANFAN_TP_COOKIE` | 到设置页粘贴 Cookie 或编辑凭据文件 |
| 请求卡住很久 | rateLimit.tpm 太低 | 设置页调高 TPM |
| 429 / RATE_LIMIT 错误 | TPM 或 RPM 超限 | 降低 rateLimit 值或增大 safetyMargin |
| settings.yaml 中文 description 显示乱码 | PowerShell 默认 GBK 编码读写 UTF-8 文件 | 读写时显式指定 `-Encoding UTF8` |
| `dsh plugin add` 报 `required option '--profile' not specified` | 指南命令缺 `--profile web` | 命令补上 `--profile web` |

---

## 七、环境信息参考

- dsh 版本：0.1.1-rc.2
- dsh-llm 版本（harness 自带）：0.1.1-rc.2（含 `LlmAdapter.prepareCall` 默认实现）
- dsh-llm 版本（插件 peer 声明）：`^0.1.1-rc.2`（commit `516f307` 提升，根治第二轮报错）
- 序列化修复：commit `12a8b9a`（serializeMessage 重写，tool_calls/tool_call_id 独立字段）
- 插件仓库分支：`feat/pr-reasoning-effort-and-tokenplan-monorepo`
- pnpm 版本：11.22.0
- Node.js 版本：v24.15.0
- 操作系统：Windows 中文系统（代码页 936/GBK）
- git 代理：`http://192.168.2.27:7890`（路由器 Clash）
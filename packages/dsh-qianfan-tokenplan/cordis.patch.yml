# dsh-qianfan-tokenplan bundle patch: one host row over the web profile.
# The row name resolves from the profile's own node_modules where
# `dsh plugin --profile web add <path>` installed this package.
- insert:
    - id: qianfan-tokenplan
      name: dsh-qianfan-tokenplan
